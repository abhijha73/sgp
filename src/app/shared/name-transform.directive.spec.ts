import { NameTransformDirective } from './name-transform.directive';

describe('NameTransformDirective', () => {
  it('should create an instance', () => {
    const directive = new NameTransformDirective();
    expect(directive).toBeTruthy();
  });
});
